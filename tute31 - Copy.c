// write a c program to find the greatest of three numbers using if-else.
#include<stdio.h>
int main()
{
    int a,b,c;
    printf("enter first number:");
    scanf("%d",&a);
    printf("enter second number:");
    scanf("%d",&b);
    printf("enter third number:");
    scanf("%d",&c);
   if( a>=b && a>=c)
   {
    printf("a is gratest");
   }
   else(b>=a&& b>=c)
   {
    printf("b is greatest");
   }
   if
   {
    printf("c is greatest");
   }
   return 0;
}

    return 0;
}